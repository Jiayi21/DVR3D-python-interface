Pytest can be installed by "pip install -U pytest", it require python version 3.7+

These tests provide verification of python source code. 

Command line interface was tested manually. Since they mainly pass argument to objects in source.

The executing of Fortran code is not automatically tested.

Tests can be run by:
~~~~
python -m pytest
~~~~

from root folder